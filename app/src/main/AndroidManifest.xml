<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jason.treadmills" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.activity.HomeActivity"
            android:label="@string/app_name"
            android:screenOrientation="landscape" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.TCoverFlowActivity"
            android:label="@string/title_activity_tcover_flow"
            android:screenOrientation="landscape" >
        </activity>
        <activity
            android:name=".ui.activity.TCarouselActivity"
            android:label="@string/title_activity_tcarousel"
            android:screenOrientation="landscape" >
        </activity>
    </application>

</manifest>
